version: "3.8"

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: next_frontend
    environment:
      - NODE_ENV=production
    expose:
      - "3000" # доступен только внутри сети Docker
    networks:
      - app_network
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: fastapi_backend
    ports:
      - "8000:8000" # теперь доступен с localhost
    environment:
      - PYTHONUNBUFFERED=1
    working_dir: /app/app
    command: uvicorn main:app --host 0.0.0.0 --port 8000
    networks:
      - app_network
    restart: unless-stopped
    volumes:
      - ./backend:/app

  redis:
    image: redis:8-alpine
    ports:
      - "6379:6379"
    container_name: redis


  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    container_name: nginx_proxy
    ports:
      - "80:80"
    depends_on:
      - frontend
      - backend
    networks:
      - app_network
    restart: unless-stopped

# 🔹 Определяем локальную сеть
networks:
  app_network:
    driver: bridge
